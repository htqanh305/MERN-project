Simple API as a backbone for user authentication and goal create, read, update, and delete. 
Main language is Express JS. API connects to MongoDB to store data. 
User's password is hashed using JWT and bcrypt
